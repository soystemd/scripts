#!/bin/sh
# Get the numeral ID (Specified by PulseAudio) of the current audio output device

CURRENTSINK=$(vol-getcurrentsinkname)
pactl list short sinks | awk -v pattern="$CURRENTSINK" '$0~pattern {print $1}'
